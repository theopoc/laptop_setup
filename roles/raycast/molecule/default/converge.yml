---
- name: Converge
  hosts: all
  tasks:
    - name: Include raycast role
      ansible.builtin.include_role:
        name: raycast
      when: ansible_facts.os_family == 'Darwin'

    - name: Display skip message for non-macOS
      ansible.builtin.debug:
        msg: "Raycast role is macOS-only and will be skipped on {{ ansible_facts.os_family }}"
      when: ansible_facts.os_family != 'Darwin'
