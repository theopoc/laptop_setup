---
# macOS-specific installation for Cursor

- name: Install Cursor via Homebrew Cask
  community.general.homebrew_cask:
    name: cursor
    state: present
    path: "{{ cursor_homebrew_path }}"
  tags: ["cursor"]
